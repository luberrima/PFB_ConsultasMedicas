import 'dotenv/config';

export const {
	MYSQL_HOST,
	MYSQL_USER,
	MYSQL_PASSWORD,
	MYSQL_DATABASE,
	MYSQL_PORT,
	ADMIN_USER,
	ADMIN_PASSWORD,
	ADMIN_EMAIL,
	FRONTEND_HOST,
	UPLOADS_DIR,
	PORT

} = process.env;
