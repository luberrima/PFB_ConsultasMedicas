import 'dotenv/config';

export const {
	MYSQL_HOST,
	MYSQL_USER,
	MYSQL_PASSWORD,
	MYSQL_DATABASE,
	MYSQL_PORT,
	ADMIN_USER,
	ADMIN_PASSWORD,
	ADMIN_EMAIL,
	SECRET,
	PORT,
	SMTP_USER,
	SMTP_API_KEY,
	UPLOADS_DIR,
	FRONTEND_HOST,
	SECRET_PATH_ADMIN,
	AVATARDIR

} = process.env;
