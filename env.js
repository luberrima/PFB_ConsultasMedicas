import 'dotenv/config';

export const {
    MYSQL_HOST,
    MYSQL_USER,
    MYSQL_PASSWORD,
    MYSQL_DATABASE,
    MYSQL_PORT,
    PORT,
    SECRET,
    FRONTEND_HOST,
    UPLOADS_DIR,
} = process.env;
